Small pill label. Use `soft`/`info` for categories, status colors for emphasis.

```jsx
<Badge variant="info">재신청</Badge>
<Badge variant="soft">일반 신청</Badge>
<Badge variant="success" dot>확정</Badge>
```

For booking lifecycle states prefer `StatusBadge`, which maps the Korean status name to the right color automatically.
