Square, borderless icon-only button for toolbar/header actions. Requires an accessible `label`.

```jsx
<IconButton label="알림"><i data-lucide="bell" /></IconButton>
<IconButton label="설정"><i data-lucide="settings" /></IconButton>
```

40×40, fog hover, slate icon. Use Lucide icons at ~20px.
